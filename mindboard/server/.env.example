# Required: powers the per-entry mood/theme analysis (Claude)
ANTHROPIC_API_KEY=

# Required: powers speech-to-text (OpenAI Whisper). Swap transcribe.ts for a
# different provider if you'd rather not use OpenAI for this step.
OPENAI_API_KEY=

PORT=4000
